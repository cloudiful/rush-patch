{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the Rush Patch application.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
